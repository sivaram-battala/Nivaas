import {fontSize, fontFamily, window, colors} from './theme';

import {
  baseURL,
  endpoints,
  methods,
  header,
  authenticatedHeader,
} from './endpoints';
import {allTexts} from './all-texts';

export {
  baseURL,
  endpoints,
  methods,
  header,
  authenticatedHeader,
  fontSize,
  fontFamily,
  window,
  colors,
  allTexts,
};
